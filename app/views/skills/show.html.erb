<p id="notice"><%= notice %></p>

<div class="row">
    <div class="col-md-6">
      <div class="thumbnail">
        <%= image_tag @skill.image.url %>
      </div>
    </div>
    <div class="col-md-6">
      <h3><%= @skill.title %></h3><br>
      <p><%= @skill.source %></p><br>
      <p><%= @skill.description %></p><br>
      <p><%= @skill.tag_list %></p><br>
      <p>Skill Track:<%= @skill.track %></P><br>
      <p>Skill Level:<%= @skill.level %></p><br>
      <p>Contact: <%= @skill.user.email %></p>
      
    </div>
</div>

<br>

<% if current_user == @skill.user %>
  <%= link_to 'Edit', edit_skill_path(@skill), class: "btn btn-link" %> |
<% end %>
<%= link_to 'Back', skills_path %>
