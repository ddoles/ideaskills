<div class="jumbotron">
  <h2> Find talented SJSU students for your project </h2>
  
</div>

<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
       <div class="col-md-3">
          <div class="thumbnail">
            <%= link_to image_tag(listing.image.url :thumb), listing %>
            <div class="caption">
              <h3><%= listing.jobtitle %></h3>
              <p><%= listing.compensation %></p><br>
              <p><%= listing.description %></p><br>
              <p><%= "Contact: #{listing.user.email}" %></p>
              <p>Skill Badges:<br><%= listing.skills %></p><br>
              <%= link_to 'Show', listing, class: "btn btn-link" %>
            </div>
          </div>
      </div>
    <% end %>
  </div>
</div>
<br>

<% if user_signed_in? %>
<%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>
